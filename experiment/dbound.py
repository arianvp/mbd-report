def dbound(transactions, itemsets):
    tau = transactions.next()
    TAU = set([tau])
    q = 1
    for tau in transactions:
        if len(tau) > q and tau is not itemsets and TODO:
            R = 
